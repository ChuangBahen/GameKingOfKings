---
description: SDD 遊戲開發流程 - Agent + SpecKit 強制整合版 v2，每步驟必須讀取前一步產出檔案
---

# SDD 遊戲開發流程（Agent + SpecKit 強制整合版 v2）

## 輸入參數
功能描述: $ARGUMENTS

---

## 🚨 強制執行規則（必讀！）

### 規則 1：必須讀取前一步產出
每個步驟開始時，**必須先用 `cat` 或 `Read` 指令讀取前一步的產出檔案**。
- ❌ 錯誤：「使用設計文件的內容」（沒有實際讀取）
- ✅ 正確：執行 `cat .claude/shared/designs/xxx.md` 並看到內容

### 規則 2：必須提取特定區塊
Step 2 必須從設計文件中提取「給 SpecKit 的輸入」區塊。

### 規則 3：必須執行 SpecKit 指令
每個步驟都要執行對應的 `/speckit.*` 指令。

### 規則 4：必須等待確認
每步完成後等待用戶輸入「繼續」。

---

## 流程總覽

```
┌────────────────────────────────────────────────────────────────┐
│                 SDD 流程 (Agent + SpecKit)                      │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  Step 0: Constitution                                          │
│     └─→ 指令: /speckit.constitution                            │
│     └─→ 產出: constitution.md                                  │
│                                                                │
│  Step 1: Design                                                │
│     └─→ Agent: game-designer 🎮                                │
│     └─→ 讀取: constitution.md ⚠️                               │
│     └─→ 產出: [feature]-design.md                              │
│            └─→ 必須包含「給 SpecKit 的輸入」區塊               │
│                                                                │
│  Step 2: Specify                                               │
│     └─→ Agent: spec-writer 📝                                  │
│     └─→ 讀取: [feature]-design.md ⚠️ 必須！                    │
│     └─→ 提取:「給 SpecKit 的輸入」區塊 ⚠️ 必須！               │
│     └─→ 指令: /speckit.specify [提取的內容]                    │
│     └─→ 產出: spec.md                                          │
│                                                                │
│  Step 3: Clarify (可跳過)                                      │
│     └─→ Agent: spec-writer 📝                                  │
│     └─→ 指令: /speckit.clarify                                 │
│                                                                │
│  Step 4: Plan                                                  │
│     └─→ Agent: tech-planner 🔧 + architect 🏗️                  │
│     └─→ 讀取: spec.md ⚠️ 必須！                                │
│     └─→ 指令: /speckit.plan [技術棧]                           │
│     └─→ 產出: plan.md, research.md                             │
│                                                                │
│  Step 5: Tasks                                                 │
│     └─→ Agent: task-breakdown 📋                               │
│     └─→ 讀取: plan.md ⚠️ 必須！                                │
│     └─→ 指令: /speckit.tasks                                   │
│     └─→ 產出: tasks.md                                         │
│                                                                │
│  Step 6: Implement                                             │
│     └─→ Agent: implementer 💻 + qa-tester 🧪                   │
│     └─→ 讀取: tasks.md ⚠️ 必須！                               │
│     └─→ 指令: /speckit.implement                               │
│     └─→ 產出: 程式碼 + 測試                                    │
│                                                                │
│  Step 7: Analyze                                               │
│     └─→ Agent: quality-reviewer ✅                             │
│     └─→ 指令: /speckit.analyze                                 │
│     └─→ 產出: 驗收報告                                         │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

---

## Step 0: Constitution

### 執行動作：

```bash
# 檢查是否存在
cat .specify/memory/constitution.md
```

如果不存在：
```
/speckit.constitution 建立遊戲專案開發原則
```

建立目錄：
```bash
mkdir -p .claude/shared/designs
```

### ✅ 完成檢查：
- [ ] constitution.md 存在
- [ ] designs 目錄存在

### 完成輸出：
```
✅ Step 0: Constitution 完成
📄 constitution.md ✓
📁 .claude/shared/designs/ ✓
➡️ 輸入「繼續」進入 Step 1
```

---

## Step 1: Design

### Agent 角色：🎮 game-designer
### SpecKit 指令：無（手動建立檔案）

### 🚨 必須執行的動作：

**1️⃣ 讀取 constitution（必須）**
```bash
cat .specify/memory/constitution.md
```

**2️⃣ 讀取遊戲設計文件（如有）**
```bash
cat .claude/shared/game-design-doc.md
```

**3️⃣ 以 game-designer 角色進行設計**

思考並記錄：
- 功能定位
- 核心循環
- 玩法機制
- 數值設計
- 玩家體驗

**4️⃣ 建立設計文件（必須使用 Write/create_file）**

### 必須建立的檔案：`.claude/shared/designs/[feature]-design.md`

```markdown
# [功能名稱] 遊戲設計

> 📅 設計日期: [日期]
> 🎮 設計者: game-designer agent

## 設計概述
- **功能定位**: [描述]
- **設計目標**: [描述]
- **成功指標**: [描述]

## 核心循環
```
[玩家行動] → [系統回饋] → [獎勵] → [動機] → [再次行動]
```

## 玩法機制

### 機制 1: [名稱]
- **描述**: 
- **目的**: 
- **觸發**: 
- **回饋**: 

### 機制 2: [名稱]
（更多機制...）

## 數值設計

| 參數 | 數值 | 類型 | 設計理由 |
|------|------|------|---------|
| | | | |

## 玩家體驗設計

### 目標情緒
[描述]

### 新手體驗
- 首次接觸: 
- 引導方式: 

### 進階體驗
- 深度: 
- 精通目標: 

## 與其他系統互動

| 系統 | 互動方式 |
|------|---------|
| | |

## 風險評估

| 風險 | 機率 | 影響 | 緩解 |
|------|------|------|------|
| | | | |

---

## 📋 給 SpecKit 的輸入（Step 2 使用）

> ⚠️ 重要：此區塊內容將直接用於 /speckit.specify 指令

**功能名稱**: [名稱]

**功能描述**: 
[詳細描述功能目的和核心價值，2-3 句話]

**使用者操作**:
1. [操作1]: [描述] - [如何觸發]
2. [操作2]: [描述] - [如何觸發]
3. [操作3]: [描述] - [如何觸發]

**數值規格**:
- [參數1]: [值] ([類型], [範圍])
- [參數2]: [值] ([類型], [範圍])

**邊界情況**:
- 當 [情況1] 時 → [處理方式]
- 當 [情況2] 時 → [處理方式]

**成功標準**:
- [標準1]
- [標準2]
```

### ✅ 完成檢查：
- [ ] 檔案已建立
- [ ] 包含「給 SpecKit 的輸入」區塊
- [ ] 區塊內容完整

### 完成輸出：
```
✅ Step 1: Design 完成 (by game-designer 🎮)
📄 檔案: .claude/shared/designs/[feature]-design.md ✓
📊 機制數: X 個
📊 數值參數: X 個
📋 「給 SpecKit 的輸入」已準備 ✓
➡️ 輸入「繼續」進入 Step 2
```

---

## Step 2: Specify

### Agent 角色：📝 spec-writer
### SpecKit 指令：`/speckit.specify`

### 🚨🚨🚨 必須執行的動作（按順序，不可省略）：

---

### 1️⃣ 讀取設計文件（必須執行！）

```bash
cat .claude/shared/designs/[feature]-design.md
```

⚠️ **你必須實際執行這個指令！**
⚠️ **不可以說「使用設計文件」而不讀取！**
⚠️ **必須在輸出中顯示檔案內容！**

---

### 2️⃣ 找到並提取「給 SpecKit 的輸入」區塊（必須執行！）

在步驟 1 讀取的內容中，找到這個標題：
```
## 📋 給 SpecKit 的輸入（Step 2 使用）
```

複製該標題下方的所有內容，包括：
- 功能名稱
- 功能描述
- 使用者操作
- 數值規格
- 邊界情況
- 成功標準

---

### 3️⃣ 執行 SpecKit 指令（必須執行！）

```
/speckit.specify [貼上步驟 2 提取的完整內容]
```

---

### 執行範例（照著做）：

```
📝 Step 2: Specify

🔍 1️⃣ 讀取設計文件
執行: cat .claude/shared/designs/minimap-design.md

[顯示檔案完整內容...]
# 小地圖優化 遊戲設計
...
## 📋 給 SpecKit 的輸入（Step 2 使用）

功能名稱: 小地圖優化

功能描述: 
優化現有小地圖系統，修正六方向顯示 Bug，增加點擊移動功能...

使用者操作:
1. 查看出口: 在小地圖上看到六個方向的出口狀態
2. 點擊移動: 點擊可用出口直接移動到該方向
...

📋 2️⃣ 提取 SpecKit 輸入
找到「給 SpecKit 的輸入」區塊，內容如下：

---
功能名稱: 小地圖優化

功能描述: 
優化現有小地圖系統...

使用者操作:
1. 查看出口: ...
2. 點擊移動: ...

數值規格:
- 出口方向數: 6

邊界情況:
- 當該方向無出口時 → 顯示灰色/禁用狀態
---

🔧 3️⃣ 執行 SpecKit 指令
/speckit.specify 功能名稱: 小地圖優化

功能描述: 
優化現有小地圖系統，修正六方向顯示 Bug，增加點擊移動功能，提升玩家導航體驗

使用者操作:
1. 查看出口: 在小地圖上看到六個方向(東西南北上下)的出口狀態
2. 點擊移動: 點擊可用出口直接移動到該方向
3. 查看房間資訊: 顯示當前房間名稱

數值規格:
- 出口方向數: 6 (東、西、南、北、上、下)

邊界情況:
- 當該方向無出口時 → 顯示灰色/禁用狀態
- 當該方向有危險時 → 顯示警告標示
```

---

### 4️⃣ 以 spec-writer 角色審查（可選）

檢查 SpecKit 產出的 spec.md：
- 是否涵蓋所有設計機制？
- 是否有遺漏的 Edge Cases？

---

### ✅ 完成檢查：
- [ ] 已執行 `cat` 讀取設計文件
- [ ] 已找到「給 SpecKit 的輸入」區塊
- [ ] 已執行 `/speckit.specify` 並帶入完整內容
- [ ] spec.md 已產生

### 完成輸出：
```
✅ Step 2: Specify 完成
   📝 Agent: spec-writer
   🔧 指令: /speckit.specify ✓
   📖 讀取: [feature]-design.md ✓
   📋 提取: 「給 SpecKit 的輸入」✓
📄 產出: .specify/specs/[NNN]-[feature]/spec.md ✓
➡️ 輸入「繼續」進入 Step 3（或「跳過」直接進入 Step 4）
```

---

## Step 3: Clarify（可跳過）

### Agent 角色：📝 spec-writer
### SpecKit 指令：`/speckit.clarify`

### 執行動作：
```
/speckit.clarify
```

回答 SpecKit 的問題。

### 跳過條件：
輸入「跳過」直接進入 Step 4。

### 完成輸出：
```
✅ Step 3: Clarify 完成
➡️ 輸入「繼續」進入 Step 4
```

---

## Step 4: Plan

### Agent 角色：🔧 tech-planner + 🏗️ architect
### SpecKit 指令：`/speckit.plan`

### 🚨 必須執行的動作：

**1️⃣ 讀取規格文件（必須）**
```bash
cat .specify/specs/*/spec.md
```
或找到正確路徑讀取。

**2️⃣ 執行 SpecKit 指令**
```
/speckit.plan 使用 [技術棧]

架構要求 (by architect):
- [設計模式]
- [模組劃分]
- [依賴注入方式]

技術要求 (by tech-planner):
- [框架版本]
- [測試框架]
- [效能考量]
```

### ✅ 完成檢查：
- [ ] 已讀取 spec.md
- [ ] 已執行 `/speckit.plan`
- [ ] plan.md 已產生

### 完成輸出：
```
✅ Step 4: Plan 完成
   🔧 Agent: tech-planner + architect
   📖 讀取: spec.md ✓
📄 產出: plan.md, research.md ✓
➡️ 輸入「繼續」進入 Step 5
```

---

## Step 5: Tasks

### Agent 角色：📋 task-breakdown
### SpecKit 指令：`/speckit.tasks`

### 🚨 必須執行的動作：

**1️⃣ 讀取計畫文件（必須）**
```bash
cat .specify/specs/*/plan.md
```

**2️⃣ 執行 SpecKit 指令**
```
/speckit.tasks
```

### ✅ 完成檢查：
- [ ] 已讀取 plan.md
- [ ] 已執行 `/speckit.tasks`
- [ ] tasks.md 已產生

### 完成輸出：
```
✅ Step 5: Tasks 完成
   📋 Agent: task-breakdown
   📖 讀取: plan.md ✓
📄 產出: tasks.md ✓
➡️ 輸入「繼續」進入 Step 6
```

---

## Step 6: Implement

### Agent 角色：💻 implementer + 🧪 qa-tester
### SpecKit 指令：`/speckit.implement`

### 🚨 必須執行的動作：

**1️⃣ 讀取任務清單（必須）**
```bash
cat .specify/specs/*/tasks.md
```

**2️⃣ 執行 SpecKit 指令**
```
/speckit.implement
```

### ✅ 完成檢查：
- [ ] 已讀取 tasks.md
- [ ] 已執行 `/speckit.implement`
- [ ] 程式碼和測試已產生

### 完成輸出：
```
✅ Step 6: Implement 完成
   💻🧪 Agent: implementer + qa-tester
   📖 讀取: tasks.md ✓
📄 產出: 程式碼 + 測試 ✓
➡️ 輸入「繼續」進入 Step 7
```

---

## Step 7: Analyze

### Agent 角色：✅ quality-reviewer
### SpecKit 指令：`/speckit.analyze`

### 執行動作：
```
/speckit.analyze
```

### 完成輸出：
```
✅ Step 7: Analyze 完成
   ✅ Agent: quality-reviewer

🎉 SDD 流程完成！
```

---

## 最終產出總覽

```
🎉 SDD + Agent + SpecKit 流程完成！

📁 產出檔案:
├── .claude/shared/designs/
│   └── [feature]-design.md     ← Step 1 (game-designer 🎮)
│
├── .specify/specs/[NNN]-[feature]/
│   ├── spec.md                 ← Step 2 (spec-writer 📝)
│   ├── plan.md                 ← Step 4 (tech-planner 🔧)
│   ├── research.md             ← Step 4
│   └── tasks.md                ← Step 5 (task-breakdown 📋)
│
├── src/...                     ← Step 6 (implementer 💻)
└── tests/...                   ← Step 6 (qa-tester 🧪)

📊 執行紀錄:
| Step | Agent | 讀取檔案 | SpecKit 指令 | 產出 |
|------|-------|---------|-------------|------|
| 0 | - | - | /speckit.constitution | constitution.md |
| 1 | 🎮 | constitution.md | (手動) | design.md |
| 2 | 📝 | ⚠️ design.md | /speckit.specify | spec.md |
| 3 | 📝 | - | /speckit.clarify | - |
| 4 | 🔧🏗️ | ⚠️ spec.md | /speckit.plan | plan.md |
| 5 | 📋 | ⚠️ plan.md | /speckit.tasks | tasks.md |
| 6 | 💻🧪 | ⚠️ tasks.md | /speckit.implement | code |
| 7 | ✅ | - | /speckit.analyze | report |
```

---

## 流程控制

| 輸入 | 效果 |
|------|------|
| `繼續` | 進入下一步 |
| `跳過` | 跳過 Step 3 |
| `回到 Step X` | 回到指定步驟 |
| `狀態` | 顯示當前進度 |

---

## 開始執行

功能描述: **$ARGUMENTS**

⚠️ **重要提醒**：
- Step 2 必須先讀取 design.md
- Step 2 必須提取「給 SpecKit 的輸入」區塊
- 每個步驟都必須執行對應的 SpecKit 指令

**準備好了嗎？讓我們開始！**
